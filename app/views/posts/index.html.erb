<div class="m-8">
  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
    <% if @posts.present? %>
      <%= render @posts %>
    <% else %>
      <div class="col-span-3 text-center text-gray-500 py-8">
        掲示板がありません
      </div>
    <% end %>
  </div>
</div>